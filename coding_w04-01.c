#include <stdio.h>

    int main() { // main(Identifier),int(Keyword)
        printf("Hello\n"); // printf(Identifier)
        printf("C Tokens!\n"); // printf(Identifier)
        return 0; // return(Keyword)
    }